# Configurações do Servidor
PORT=5000
NODE_ENV=development

# MongoDB - ESCOLHA UMA DAS OPÇÕES:

# OPÇÃO 1: MongoDB Atlas (Nuvem - RECOMENDADO)
# Crie uma conta grátis em: https://www.mongodb.com/cloud/atlas
# Depois cole a string de conexão aqui:
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/reveste?retryWrites=true&w=majority

# OPÇÃO 2: MongoDB Local (se tiver instalado)
# MONGODB_URI=mongodb://localhost:27017/reveste

# JWT Secret (use qualquer string aleatória)
JWT_SECRET=seu_secret_super_secreto_aqui_mude_isso

# Configurações de Upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
